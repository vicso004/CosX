<p>Hej <%= @assignment.player.name.split.first%>!</p>

<p>Nu är det dags igen för match, denna gång mot <%= @assignment.match.other_team%>.</p> 
<p>Det uttagna laget är :</p> 
<ul>
<% @assignment.match.players.each do |player| %>
<li><%= link_to player.name, player_url(player) %></li>
<%end%>
</ul>
<p>Bekräfta att du kommer genom att klicka på
  <%= link_to 'denna länk', match_assignments_url+"/"+@assignment.id.to_s+"/confirm", :metod => :put, :action => :confirm%></p>
  <p>Om du inte kan komma komma så klickar du <%= link_to 'här', match_assignments_url+"/"+@assignment.id.to_s+"/deny", :metod => :put, :action => :deny%></p>
  
  
  
