<%= simple_form_for @post, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <%= f.input :title %>
    <%= f.input :body %>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Avbryt', posts_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
