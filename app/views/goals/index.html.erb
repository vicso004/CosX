<h1>Listing goals</h1>

<table>
  <tr>
    <th>Match</th>
    <th>Goaler</th>
    <th>Assister</th>
    <th></th>

  </tr>

<% @goals.each do |goal| %>
  <tr>
    <td><%= goal.match_id %></td>
    <td><%= goal.goaler_id %></td>
    <td><%= goal.assister_id %></td>
    <td><%= link_to 'Show' %></td>
   
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Goal', new_match_goal_path %>
