<p id="notice"><%= notice %></p>

<p>
  <b>Titel</b>
  <%= @article.title %>
</p>

<p>
  <b>Innehåll</b>
  <%= @article.content %>
</p>

<% @article.assets.each do |asset| %>
	<%= link_to( image_tag(asset.image.url(:thumb)), asset.image.url(:large), :rel =>"group", :class => "fancybox") %>                  
<% end %>   

<p>
Klicka på bilderna för att starta bildvisning	  
</p>              

 <p>  
<%= link_to 'Uppdatera', edit_article_path(@article), class: "btn btn-mini" %> |
<%= link_to 'Tillbaka', articles_path, class: "btn btn-mini" %>
</p>  